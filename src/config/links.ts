// External links woven into generated copy.

/** Newsletter signup URL that replaces the literal "[SUBSCRIBE LINK]" placeholder
 *  at send time (the generator emits the placeholder; it must never publish raw). */
export const SUBSCRIBE_URL = 'https://letsgo.takeo.co';

/** Literal placeholder the copywriter inserts for the newsletter CTA. */
export const SUBSCRIBE_PLACEHOLDER = '[SUBSCRIBE LINK]';
